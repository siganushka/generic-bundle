{% extends 'base.html.twig' %}

{% block body %}
  <div class="row">
    <div class="col text-truncate">
      <ol>
        {% for alias, item in types %}
          <li>
            <a href="{{ path('siganushka_generic_form', {alias}) }}" title="{{ constant('class', item) }}">{{ constant('class', item) }}</a>
          </li>
        {% else %}
          <li>No forms.</li>
        {% endfor %}
      </ol>
    </div>
    {% if form %}
      <div class="col-12 col-md-7">
        <div class="border rounded bg-light p-3 flex-grow-1">
          {{ form(form) }}
        </div>
      </div>
    {% endif %}
  </div>
{% endblock %}
